Drop Apollo CSV exports here as `apollo-batch-01.csv`, etc.

Required columns (any order): first_name, company, website/domain, email,
email_status, title, employees.

Only rows with a verified email status get contacted. See
`ops/outreach/apollo-playbook.md`.
